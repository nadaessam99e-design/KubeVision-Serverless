apiVersion: v1
kind: Service
metadata:
  name: platform-api-svc
  namespace: platform-apps
  labels:
    app: platform-api
spec:
  type: ClusterIP 
  selector:
    app: platform-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080